# Formspree endpoint used by the contact form (src/components/ContactForm.jsx).
# Create a form at https://formspree.io and paste its endpoint here, then restart
# the dev server. If unset, the form falls back to the default portfolio endpoint.
REACT_APP_FORMSPREE_ENDPOINT=your_formspree_endpoint_here
